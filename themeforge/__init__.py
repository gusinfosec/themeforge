"""ThemeForge — a Theme Studio-style one-click desktop theming app for GNOME.

Works on Ubuntu, Fedora and Arch (GNOME desktop). Installs GTK, GNOME Shell
and icon themes into user directories only — no root, no package manager.
"""

__version__ = "0.1.0"
